apiVersion: v1
kind: Service
metadata:
  name: sniply-auth
  namespace: sniply-staging

spec:
  selector:
    app: sniply-auth

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001

  type: ClusterIP