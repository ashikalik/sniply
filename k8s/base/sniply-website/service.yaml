apiVersion: v1
kind: Service
metadata:
  name: sniply-website
  namespace: sniply-staging

spec:
  selector:
    app: sniply-website

  ports:
    - protocol: TCP
      port: 80
      targetPort: 4200

  type: ClusterIP