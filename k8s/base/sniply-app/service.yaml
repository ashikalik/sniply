apiVersion: v1
kind: Service
metadata:
  name: sniply-app
  namespace: sniply-staging

spec:
  selector:
    app: sniply-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 4201

  type: ClusterIP